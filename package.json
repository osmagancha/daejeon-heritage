{
  "name": "hanbat-giheng",
  "version": "1.0.0",
  "description": "한밭기행 — 대전 문화유산 인터랙티브 투어 플랫폼",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --watch --env-file-if-exists=.env server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
